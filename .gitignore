# .gitignore 파일 내용

# 1. 파이참(PyCharm)이 자동으로 만드는 설정 폴더
# (님이 이전에 궁금해하셨던 .idea 폴더입니다!)
.idea/

# 2. 앞으로 만들 모든 '연습용' 파일
# (이름에 _practice가 들어간 파일은 모두 무시됩니다)
*_practice.py
practice_*.py

# 3. (선택) 특정 이름의 파일 무시